# shamefully-hoist=true is required because several packages in this project
# (e.g. MUI, Emotion, Tailwind plugins) expect to resolve peer dependencies
# from node_modules root rather than through pnpm's default isolated layout.
# Without this, those packages fail at runtime or during the build step.
shamefully-hoist=true
